import styled from "@emotion/styled";

const DialogBackdrop = styled.div({
    backgroundColor: "#000",
    position: "absolute",
    top: 0,
    left: 0,
    width: "100%",
    height: "100%",
    opacity: 0.6,
});

export { DialogBackdrop };
