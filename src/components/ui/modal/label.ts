import styled from "@emotion/styled";

const DialogLabel = styled.h3({
    margin: 0,
    padding: "1rem",
    borderBottom: "0.125rem solid lightgray",
    position: "relative",
    fontSize: "1.5rem",
});

export { DialogLabel };
